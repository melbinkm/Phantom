# SPDX-License-Identifier: GPL-2.0-only
#
# Kbuild — kernel object rules for phantom.ko
#

obj-m := phantom.o

phantom-objs := \
	phantom_main.o \
	vmx_core.o \
	ept.o \
	ept_cow.o \
	snapshot.o \
	memory.o \
	interface.o \
	debug.o \
	hypercall.o \
	nmi.o \
	pt_config.o

# Enable debug assertions and trace_printk during development.
# Remove -DPHANTOM_DEBUG for production builds.
ccflags-y := -DPHANTOM_DEBUG -Wall -Werror
